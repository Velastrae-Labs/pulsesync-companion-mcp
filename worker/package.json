{
  "name": "pulsesync-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Reference Cloudflare Worker for PulseSync Advanced Export — user-owned ingest + read API.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
